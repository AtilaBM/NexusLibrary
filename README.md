# Nexux Library

Nexux Library is a **study project** created to practice **Java OOP with JDBC** and **PostegreSQL** concepts.
It is a simple library system that runs in the terminal (CLI) and simulates basic library operations.